/*
 * MultiSelection Messages
 *
 * This contains all the text for the MultiSelection component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.MultiSelection';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MultiSelection component!',
  },
});
